# Improved Inventory 0.1.0-beta.2 local candidate

25 September 2026. Renames the former Inventory QoL candidate; no public
release or tag has been created. Gameplay and UI behavior are unchanged.

- Source commit: `f5cd001a4884b7991a075fb084ccdf57fc763348`.
- Archive: `ImprovedInventory-0.1.0-beta.2.zip`.
- SHA-256: `5bb2999d1f8b3ed9cd3f95dcec6c922e8bfe1a9285622819065b4bf30104c66f`.
- Local archive, sidecar and manifest:
  `outputs/releases/improved-inventory/0.1.0-beta.2/`.
- [GitHub checks passed](https://github.com/anitosq/mewgenics-mods/actions/runs/36089312600).

The rename includes the display name, source/documentation folders, build and
test entry points, CI name, future tag prefix, exported DLL identity, log and
hook labels, diagnostic environment switch/session filename, DLL and asset
paths, and browser preview branding. Internal `iq_` implementation identifiers
are unchanged; they are not player-facing mod names.

The new packager passed 9 Python, 4 Node and 4 native fixtures, asset generation,
native font/art validation, release compilation, executable identity checks and
enabled-path/matching-asset guard checks. The diagnostic build and its image
guard also passed. The installed Vortex extension accepted the renamed mixed
DLL/asset archive through the installer-function check.

Removed the previous installed candidate through Vortex, retaining its archive,
then installed the new ZIP using Install From File. Verified all eight deployed
payload files against the manifest (Vortex excludes CHANGELOG.md as before).
The old `mods/InventoryQoL.dll` and `mods/InventoryQoL/` are absent. Vortex's
generated launch command includes `ImprovedInventory` and no old asset path.
The loader configuration remains byte-identical to the previous test backup.

The game was not launched during the rename. The previous
[in-game smoke test](0.1.0-beta.1-candidate.md) applies to the pre-rename artifact;
it is not a new gameplay test of beta.2. Finish the remaining
[release checklist](../../../mods/improved-inventory/RELEASE_CHECKLIST.md),
including in-game startup of the renamed package, before publication.

Historical archives/checksums and exact log excerpts preserve their original
names. Current installation and migration instructions are in
[INSTALL.md](../../../mods/improved-inventory/INSTALL.md).
