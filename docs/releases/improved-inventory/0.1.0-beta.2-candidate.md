# Improved Inventory 0.1.0-beta.2 local candidate

25 September 2026. Renames the former Inventory QoL candidate; no public
release or tag has been created. Gameplay and UI behavior are unchanged.

- Source commit: `f5cd001a4884b7991a075fb084ccdf57fc763348`.
- Archive: `ImprovedInventory-0.1.0-beta.2.zip`.
- SHA-256: `5bb2999d1f8b3ed9cd3f95dcec6c922e8bfe1a9285622819065b4bf30104c66f`.
- Local archive, sidecar and manifest:
  `outputs/releases/improved-inventory/0.1.0-beta.2/`.
- [GitHub checks passed](https://github.com/anitosq/mewgenics-mods/actions/runs/36089312600).

The rename includes the display name, source/documentation folders, build and
test entry points, CI name, future tag prefix, exported DLL identity, log and
hook labels, diagnostic environment switch/session filename, DLL and asset
paths, and browser preview branding. Internal `iq_` implementation identifiers
are unchanged; they are not player-facing mod names.

The new packager passed 9 Python, 4 Node and 4 native fixtures, asset generation,
native font/art validation, release compilation, executable identity checks and
enabled-path/matching-asset guard checks. The diagnostic build and its image
guard also passed. The installed Vortex extension accepted the renamed mixed
DLL/asset archive through the installer-function check.

Removed the previous installed candidate through Vortex, retaining its archive,
then installed the new ZIP using Install From File. Verified all eight deployed
payload files against the manifest (Vortex excludes CHANGELOG.md as before).
The old `mods/InventoryQoL.dll` and `mods/InventoryQoL/` are absent. Vortex's
generated launch command includes `ImprovedInventory` and no old asset path.
The loader configuration remains byte-identical to the previous test backup.

The game was not launched during the rename. The user subsequently confirmed
the renamed build worked, then completed an isolated-profile test below.

## Isolated user test, 25 September 2026

Reviewed the 11:20:28–11:22:31 local-time loader log after the user completed
the test. The game was closed when checked. Deployed files still match the
frozen beta.2 manifest. Evidence was retained privately under
`work/backups/isolated-user-test-20260925-112028/`.

- Mewjector v3.0/API3 loaded exactly one mod DLL, ImprovedInventory.dll.
  Executable hash and integrity checks passed; all UI hooks installed.
- The current Vortex-generated launch command and modlist contain only
  ImprovedInventory's asset folder. Together with the successful asset gate,
  this supports the isolated-profile test configuration.
- The shared configuration retains the disabled BiggerWallet priority entry.
  Its load attempt failed with error 126; no BiggerWallet hooks loaded. The
  configuration was left intact for switching back to the regular profile.
- Drawer counts started at 92 Storage / 0 Trash. Trash reached 37 and scrolled
  repeatedly between rows 0 and 1. Storage scrolling was also recorded. Counts
  returned to 92/0; every logged drawer total remained 92.
- Search input (up to six characters), all type filters, four rarity filters,
  set selections, empty results and resets were recorded. Logs establish
  exercised actions/counts, not independent visual or sound verification.
- No exit exception was recorded and no crash report exists for this process.
  Earlier automation-assisted runs recorded an exception with raw stack
  candidates in UIAutomationCore, including without our DLL. This run does
  not establish the cause of the earlier exception.

This verifies operation with only the loader and this mod active on the
existing installation; it is not a fresh game/loader installation test. No
save rollback was performed. This session's save slot, backup status and
save/reload behavior were not independently verified and are not claimed as
completed. Finish the remaining
[release checklist](../../../mods/improved-inventory/RELEASE_CHECKLIST.md)
before publication. The [earlier smoke test](0.1.0-beta.1-candidate.md) remains
evidence for the pre-rename archive only.

## Vortex lifecycle user test, 25 September 2026

The user reported successful completion of all four instructed sequences:
disable/deploy/launch (original UI), re-enable/deploy/launch (mod UI),
remove/deploy/launch (original UI), and reinstall/deploy/launch (mod UI).
The game was closed after testing. Earlier step logs were overwritten by
later launches, so those outcomes are recorded as user verification.

Independently inspected the final reinstall log beginning at 11:34:08 local
time. It identifies beta.2, validates the executable, installs all UI hooks,
creates the 92/0 inventory view and records scrolling. Exactly one mod DLL
loaded; the only deployed DLL under mods is ImprovedInventory.dll. All eight
deployed payload files match the frozen manifest. The asset modlist contains
only ImprovedInventory; loader configuration is unchanged from the isolated
test. As before, the disabled BiggerWallet priority entry failed to load.
No exit exception or matching crash report was recorded for this process.

Retained final-run evidence privately under
`work/backups/reinstall-user-test-20260925-113408/`. These results do not cover
unchecking only the asset Load Order entry, a same-name version upgrade, or
explicit purge/redeploy. No saves were modified or restored by this review.

Historical archives/checksums and exact log excerpts preserve their original
names. Current installation and migration instructions are in
[INSTALL.md](../../../mods/improved-inventory/INSTALL.md).
