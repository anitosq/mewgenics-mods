# Improved Inventory 0.2.0 publication record

Published on 25 September 2026, authorized after the user's confirmation that
the installed performance build works well. Both hosts carry the exact tested
archive, verified by downloads. Nexus visibility is restored.

## Frozen artifact

- Source commit: `c646edce3300cf7363be42bf48d568dfab7bce82`.
- Annotated tag: `improved-inventory/v0.2.0`, pointing at that tested commit.
- Archive: `ImprovedInventory-0.2.0.zip`, 137914 bytes.
- Local package: `outputs/releases/improved-inventory/0.2.0-performance/`.
- SHA-256: `891513bdd58ce37d6cee4df1d2fd60fe1462d26e86747586feeed553c771854e`.
- The same frozen archive is used on both hosts. Publication copy and release
  dates on main were updated separately; the tested payload was not rebuilt.
  The packaged changelog still labels 0.2.0 as unreleased.
- Build, fixture and runtime results are in the [candidate record](0.2.0-candidate.md).
  Vortex replacement and deployed hashes passed; the user accepted the result.
  An explicit upgrade from public 0.1.0 remains distinct from these tests.

## Destinations

- [GitHub release](https://github.com/anitosq/mewgenics-mods/releases/tag/improved-inventory/v0.2.0).
- [Nexus listing](https://www.nexusmods.com/mewgenics/mods/526).
- Nexus game ID `8802`, mod ID `526`, existing API file ID `8024283`.
- Nexus mod and file display names remain `Improved Inventory`.
- GitHub published at `2026-09-25T08:43:05Z`, with draft and prerelease false.
  Uploaded ZIP and checksum sidecar; publication used `--latest=false`.
- Nexus version 0.2.0 is Main/primary with mod-manager downloads enabled.
  Legacy file/version ID `1655`, unique version ID `37804302141047`, short ID
  `aPrMPfLXO`. The 0.1.0 version remains under Old files.
- The page was hidden by the author while the Adventure crash was investigated.
  Set mod to visible restored public access after the file and copy updates.
  The hidden banner disappeared and the page shows version 0.2.0.

## Listing and media

Reviewed public copy with the humanizer guidance. The summary, full description,
file notes, GitHub notes and repository README now describe pre-adventure
equipment selection. Release notes describe the crash fix and new controls.

The author supplied `Screenshot 2026-09-25 163034.png` from Downloads for the
gallery. Inspected it before upload: equipment selection with Very Rare active,
seven items visible, and the search/type/rarity/set controls. No claim is made
about other mods enabled during this capture. The existing cover remains the
thumbnail. The gallery now has five images, including the new caption
"Find equipment for your cats before an adventure".

## Verification

- Local archive and all nine payload hashes match the frozen manifest.
- GitHub's asset digest and a fresh authenticated download match the archive
  SHA-256 and all payload hashes.
- Nexus reports Safe to use; its VirusTotal link identifies the same SHA-256.
  Edge again blocked the manual CDN download with `ERR_BLOCKED_BY_CLIENT`.
  No browser protections were changed.
- The Nexus-to-Vortex download succeeded. The downloaded archive
  `Improved Inventory 526 0.2.0 2026-09-25T08-37Z aPrMPfLXO.zip` matches the
  frozen archive byte for byte and all nine payload hashes pass.
- Vortex showed the Nexus entry enabled as Improved Inventory, version 0.2.0,
  in the user's Test profile. It also had the local test entry enabled. Disabled
  that duplicate; the published entry remains enabled and the conflict notice
  cleared. All eight deployed files match; Vortex omits CHANGELOG.md. One DLL
  remains deployed. No game was launched or save changed in this publication.
- [CI](https://github.com/anitosq/mewgenics-mods/actions/runs/36114175577) passed
  on the publication documentation commit, which contains the tagged code.

Synchronization is verified. Broader runtime coverage and an explicit upgrade
from public 0.1.0 remain recorded separately in the candidate/checklist.
